1- create in app folder new group (Auth)
2- create in (Auth) two folder sign-in and sign-up inside all of them page.tsx

___________________________________________________________________________________________________
implement Auth with NextAuth Auth.js

1-npm install next-auth@beta
2-npx auth secret

3- make file name it auth.js 

import NextAuth from "next-auth"
 
export const { handlers, signIn, signOut, auth } = NextAuth({
  providers: [],
})

4-add multi folder in app as /app/api/auth/[...nextauth]/route.ts

in route.ts write 

import { handlers } from "@/auth" // Referring to the auth.ts we just created
export const { GET, POST } = handlers

5- add new file name it middleware.ts inside it 

export { auth as middleware } from "@/auth"

6- we need to add providers from github we add it by go to 

gitub/profile/settings/Developer settings/OAuth Apps/new OAuth Apps:
Application name =devflow
Homepage URL =http://localhost:3000
Authorization callback url = http://localhost:3000/api/auth/callback/github

7- now copy client ID and Client secrets

in env loacl we past client side and Client secrets value 

AUTH_GITHUB_ID=Client ID value 
AUTH_GITHUB_SECRET=Client secrets value 

8-we need to add providers from google from console.cloud.google.com

newProject==>devFlow==>create==>from navigation menu ==>api&serviece ==>OAuth consent screen==>
oveview==>getStarted==>name and email write ==>external==>another email ==>
Create OAuth client ID==>Application type= Web application==> add Url =http://localhost:3000

9-now copy client ID and Client secrets

AUTH_GOOGLE_ID=Client ID value 
AUTH_GOOGLE_SECRET=Client secrets value 

10- in auth file we add 

import NextAuth from "next-auth"
import GitHub from "next-auth/providers/github"
import Google from "next-auth/providers/google"

export const { handlers, signIn, signOut, auth } = NextAuth({
    providers: [GitHub,Google],
})
